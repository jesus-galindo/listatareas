/** aqui se importan todas las clases de esta 
 * carpeta, y al final se exportan para que en el
 * index.js de la carpeta raiz, solo esté un index
 */

import { Todo } from './todo.class';
import { TodoList } from './todo-list.class';

export {
    Todo,
    TodoList,
}
 
 